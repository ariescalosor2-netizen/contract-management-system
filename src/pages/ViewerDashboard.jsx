import { useEffect, useState } from "react";
import { BiFile, BiGroup, BiTime, BiCheckCircle } from "react-icons/bi";
import MainLayout from "../layouts/MainLayout";
import api from "../services/api";

export default function ViewerDashboard(){const [d,setD]=useState(null),[error,setError]=useState("");useEffect(()=>{api.get("/viewer/dashboard").then(r=>setD(r?.data?.data||null)).catch(e=>setError(e.response?.data?.detail||"Failed to load viewer dashboard."))},[]);const cards=[["Total Contracts","total_contracts",BiFile],["Active Contracts","active_contracts",BiCheckCircle],["Expiring Soon","expiring_contracts",BiTime],["Total Parties","total_parties",BiGroup]];return <MainLayout><div className="mb-6"><h1 className="text-3xl font-bold text-gray-900">Viewer Dashboard</h1><p className="mt-1 text-gray-500">Read-only overview of your organization.</p></div>{error&&<div className="mb-5 rounded-lg border border-red-200 bg-red-50 px-4 py-3 text-sm text-red-600">{error}</div>}<div className="grid grid-cols-1 gap-5 sm:grid-cols-2 xl:grid-cols-4">{cards.map(([t,k,I])=><div key={k} className="rounded-2xl border border-gray-100 bg-white p-5 shadow-sm"><div className="mb-4 flex h-11 w-11 items-center justify-center rounded-xl bg-blue-50 text-2xl text-blue-600"><I/></div><p className="text-sm text-gray-500">{t}</p><p className="mt-1 text-2xl font-bold text-slate-800">{d?.[k]??"..."}</p></div>)}</div><div className="mt-6 overflow-hidden rounded-2xl border border-gray-100 bg-white shadow-sm"><div className="border-b px-6 py-5"><h2 className="font-semibold text-slate-800">Recent Contracts</h2><p className="mt-1 text-sm text-gray-500">View-only contract information.</p></div><div className="overflow-x-auto"><table className="w-full text-left"><thead className="bg-gray-50 text-xs uppercase text-gray-400"><tr><th className="px-5 py-4">Contract</th><th className="px-5 py-4">Title</th><th className="px-5 py-4">Status</th><th className="px-5 py-4">End Date</th></tr></thead><tbody>{(d?.recent_contracts||[]).map(x=><tr key={x.id} className="border-t"><td className="px-5 py-4 font-semibold">{x.contract_no}</td><td className="px-5 py-4">{x.title}</td><td className="px-5 py-4">{x.status}</td><td className="px-5 py-4">{x.end_date}</td></tr>)}</tbody></table></div></div></MainLayout>}
