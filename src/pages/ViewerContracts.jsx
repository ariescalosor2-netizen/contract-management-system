import { useEffect, useState } from "react";
import { useNavigate } from "react-router-dom";
import MainLayout from "../layouts/MainLayout";
import { getContracts } from "../services/contractService";

export default function ViewerContracts(){const nav=useNavigate();const [rows,setRows]=useState([]),[loading,setLoading]=useState(true),[error,setError]=useState("");useEffect(()=>{getContracts().then(r=>setRows(r?.data||[])).catch(e=>setError(e.response?.data?.detail||"Failed to load contracts.")).finally(()=>setLoading(false))},[]);return <MainLayout><div className="mb-6"><h1 className="text-3xl font-bold text-gray-900">Contracts</h1><p className="mt-1 text-gray-500">Read-only contract information.</p></div>{error&&<div className="mb-5 rounded-lg border border-red-200 bg-red-50 px-4 py-3 text-sm text-red-600">{error}</div>}<div className="overflow-hidden rounded-2xl border border-gray-100 bg-white shadow-sm"><div className="overflow-x-auto"><table className="w-full text-left"><thead className="bg-gray-50 text-xs uppercase tracking-wide text-gray-400"><tr><th className="px-5 py-4">Contract No.</th><th className="px-5 py-4">Title</th><th className="px-5 py-4">Party</th><th className="px-5 py-4">Status</th><th className="px-5 py-4">End Date</th></tr></thead><tbody>{rows.map(r=><tr key={r.id} onClick={()=>nav(`/viewer/contracts/${r.id}`)} className="cursor-pointer border-t border-gray-100 hover:bg-gray-50"><td className="px-5 py-4 font-semibold text-slate-800">{r.contract_no}</td><td className="px-5 py-4">{r.title}</td><td className="px-5 py-4">{r.party_name||"-"}</td><td className="px-5 py-4">{r.status}</td><td className="px-5 py-4">{r.end_date}</td></tr>)}{!loading&&!rows.length&&<tr><td colSpan="5" className="px-5 py-10 text-center text-sm text-gray-500">No contracts found.</td></tr>}</tbody></table></div></div></MainLayout>}
