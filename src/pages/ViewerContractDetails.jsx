import { useEffect, useState } from "react";
import { useNavigate, useParams } from "react-router-dom";
import MainLayout from "../layouts/MainLayout";
import { getContract } from "../services/contractService";
import { getContractParties } from "../services/contractPartyService";

export default function ViewerContractDetails(){const {contractId}=useParams();const nav=useNavigate();const [c,setC]=useState(null),[parties,setParties]=useState([]),[error,setError]=useState("");useEffect(()=>{Promise.all([getContract(contractId),getContractParties(contractId)]).then(([a,b])=>{setC(a?.data||null);setParties(b?.data||[])}).catch(e=>setError(e.response?.data?.detail||"Failed to load contract."))},[contractId]);return <MainLayout><button onClick={()=>nav("/viewer/contracts")} className="mb-5 text-sm font-semibold text-blue-600">← Back to Contracts</button>{error&&<div className="mb-5 rounded-lg border border-red-200 bg-red-50 px-4 py-3 text-sm text-red-600">{error}</div>}{c&&<><div className="mb-6"><h1 className="text-3xl font-bold text-gray-900">{c.contract_no}</h1><p className="mt-1 text-gray-500">{c.title}</p></div><div className="grid grid-cols-1 gap-5 md:grid-cols-2"><div className="rounded-2xl bg-white p-6 shadow-sm"><p className="text-xs uppercase text-gray-400">Status</p><p className="mt-2 font-semibold">{c.status}</p><p className="mt-5 text-xs uppercase text-gray-400">Contract Type</p><p className="mt-2">{c.contract_type_name||"-"}</p></div><div className="rounded-2xl bg-white p-6 shadow-sm"><p className="text-xs uppercase text-gray-400">Start Date</p><p className="mt-2">{c.start_date}</p><p className="mt-5 text-xs uppercase text-gray-400">End Date</p><p className="mt-2">{c.end_date}</p></div></div><div className="mt-6 rounded-2xl bg-white shadow-sm"><div className="border-b px-6 py-5"><h2 className="font-semibold text-slate-800">Parties</h2></div>{parties.map(p=><div key={p.id} className="flex items-center justify-between border-b px-6 py-4 last:border-0"><div><p className="font-semibold">{p.party_name}</p><p className="text-sm text-gray-500">{p.party_type||"-"}</p></div><span className="rounded-full bg-blue-50 px-3 py-1 text-xs font-semibold text-blue-700">{p.role}</span></div>)}</div></>}</MainLayout>}
