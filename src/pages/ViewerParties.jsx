import { useEffect, useState } from "react";
import MainLayout from "../layouts/MainLayout";
import { getParties } from "../services/partyService";

export default function ViewerParties(){const [rows,setRows]=useState([]),[error,setError]=useState("");useEffect(()=>{getParties().then(r=>setRows(r?.data||[])).catch(e=>setError(e.response?.data?.detail||"Failed to load parties."))},[]);return <MainLayout><div className="mb-6"><h1 className="text-3xl font-bold text-gray-900">Parties</h1><p className="mt-1 text-gray-500">Read-only party information.</p></div>{error&&<div className="mb-5 rounded-lg border border-red-200 bg-red-50 px-4 py-3 text-sm text-red-600">{error}</div>}<div className="overflow-hidden rounded-2xl border border-gray-100 bg-white shadow-sm"><div className="overflow-x-auto"><table className="w-full text-left"><thead className="bg-gray-50 text-xs uppercase tracking-wide text-gray-400"><tr><th className="px-5 py-4">Party</th><th className="px-5 py-4">Type</th><th className="px-5 py-4">Email</th><th className="px-5 py-4">Contact</th><th className="px-5 py-4">Status</th></tr></thead><tbody>{rows.map(r=><tr key={r.id} className="border-t border-gray-100"><td className="px-5 py-4 font-semibold text-slate-800">{r.name}</td><td className="px-5 py-4">{r.type}</td><td className="px-5 py-4">{r.email||"-"}</td><td className="px-5 py-4">{r.contact||"-"}</td><td className="px-5 py-4">{r.status}</td></tr>)}</tbody></table></div></div></MainLayout>}
